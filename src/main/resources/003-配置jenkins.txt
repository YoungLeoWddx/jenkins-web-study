1，配置全局属性。
	jenkins系统管理--系统配置：可以配置jenkins运行过程中的全局属性，使用key value对的是形式。可以使用${name}或者$name的方式
	在构建脚本中使用它们。也可以在执行mvn命令的时候，通过参数传递给maven。
	
2，jenkins支持三个主流构建工具，ant，maven，和基本的shell脚本。我们未来都是使用maven来进行构建。


3，配置系统管理员邮件地址，这里配置773958125@qq.com，这个地址是发送通知右键的发送方。


4，jenkins一般在内网中，不能上网，所以需要配置代理服务器来上网，具体看，jenkins权威指南101页。


5，