jenkins中构建一个项目有两个比较特殊的选项：
	该项目的上游项目正在构建时阻止该项目构建：
		选中此选项后，当该项目的依赖项在队列或构建中时，Jenkins将阻止项目构建。 依赖关系包括直接和传递依赖。
		我们知道，如果采用了聚合工程，不存在这种问题，因为maven的聚合工程会有自己的反应堆，他知道怎么按照依赖关系来构建，但是如果依赖的
		两个项目是独立开来的两个maven项目呢？那么，必须等待上游maven项目构建完毕了，才可以构建下游的maven项目。
	该项目的下游项目正在构建时阻止该项目构建：
		和上面的完全相反。