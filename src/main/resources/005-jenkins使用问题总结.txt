1,当jenkins采用轮询版本库的时候，如果jenkins在进行构建的时候，有新的代码提交，这个时候，jenkins会并行执行构建吗？
	
2,CI服务器构建失败的时候，应该拒绝新的代码提交和代码签出，但是CI服务器轮询之前在构建失败这个过程中怎么阻止代码提交和签出？jenkins是如何做到这种控制的？

3,当需要构建的代码越来越庞大，构建越来越频繁，CI服务器Jenkins如何突破性能上的瓶颈？

4，Jenkins如何获取构建的度量数据，比如编译时间？源码行数？审查的种类数，时间，测试执行的时间，打包，发布时间？重建数据库的时间，版本控制系统的负载？CI服务器的
系统资源使用情况？成功构建和失败构建的比例？

5，jenkins分布式构建是什么意思，怎么做？

6，jenkins在全量构建很耗时的时候是否支持增量构建？

7，jenkins如何处理聚合项目的构建？某个子项目代码有变更，其他子项目或者父聚合项目是否会被重新构建？

8，jenkins是否支持分段构建？

9，版本库变更就执行构建和定期构建之间如何选择？

10，如果源代码分散在多个不同的版本控制中呢？比如不同的子系统源码，jenkins该如何处理？

11，jenkins如何做提交构建和次级构建规划？

12，jenkins中如何做到测试覆盖率检查？和自动反馈？

13，jenkins如何实时显示构建的状态？

14，jenkins如何跟踪某次构建失败是由谁的新提交造成的？

15，jenkins中的反馈机制，怎么通知到指定的人？


16，在jenkins构建失败的时候，怎么组织新的代码提交和代码检出？


17，jenkins如何调度新的提交和构建触发之间的关系？


18，jenkins中是在出错就停止构建还是某个构建阶段完成后汇总错误信息？